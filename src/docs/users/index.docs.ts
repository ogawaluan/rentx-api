/**
 * @swagger
 * components:
 *   schemas:
 *     User:
 *       type: object
 *       properties:
 *         id:
 *           type: string
 *         name:
 *           type: string
 *         email:
 *           type: string
 *         images:
 *           type: object
 *           properties:
 *             large:
 *               type: string
 *             original:
 *               type: string
 *         facebookId:
 *           type: string
 *         facebookImage:
 *           type: string
 *         googleId:
 *           type: string
 *         googleImage:
 *           type: string
 *         appleId:
 *           type: string
 *         createdAt:
 *           type: string
 *       required:
 *         - id
 *         - createdAt
 */
